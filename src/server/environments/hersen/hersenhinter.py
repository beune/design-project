"""
Imports
"""
from src.model.report_node import ReportNode

hintdict = {}


def hint(root_node: ReportNode):
    """
    Method used to add hints for brain scan reports
    :param root_node: The root node of the report
    :return: For now just the root node, hersenhinter and nlp are not implemented
    """
    return root_node
